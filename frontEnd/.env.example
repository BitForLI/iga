# 复制为 .env 后按需填写。Vite 仅暴露以 VITE_ 开头的变量。

# 后端 API（须带 /api，与 ASP.NET 路由一致）
# 本地：
# VITE_API_BASE=http://127.0.0.1:5212/api
# Railway 等生产构建时改为真实公网地址，例如：
# VITE_API_BASE=https://你的服务名.up.railway.app/api

# 可选：商品图 /uploads 等静态资源主机；不填则从 VITE_API_BASE 自动去掉末尾 /api
# VITE_API_ORIGIN=https://你的服务名.up.railway.app

# VITE_MAPBOX_ACCESS_TOKEN=
