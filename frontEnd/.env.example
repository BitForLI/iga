# 复制为 .env 后按需填写。Vite 仅暴露以 VITE_ 开头的变量。

# --- 本地开发（默认不写也可：apiEnv.ts 会回落到 localhost:5212）---
# VITE_API_BASE=http://127.0.0.1:5212/api
# VITE_API_ORIGIN=http://127.0.0.1:5212

# --- 生产构建（托管平台「构建环境变量」里设置；勿把真实密钥写进仓库）---
# 后端 API：须带 /api，填你在 Railway 上的后端公网地址（或日后 api 子域）
# VITE_API_BASE=https://你的后端.up.railway.app/api
# 可选：仅当静态资源与 API 不同域时再设
# VITE_API_ORIGIN=https://你的后端.up.railway.app

# 可选：顾客站点域名（与 Stripe 支付完成回跳、后端 CORS 一致；不设则用 constants/site.ts 默认）
# VITE_PUBLIC_SITE_ORIGIN=https://www.igabeverlyhills.com

# Mapbox 地址联想（配送抽屉内「街道地址」自动补全）
# 1) 在 https://account.mapbox.com/ 创建 Access token（公开 token 一般为 pk. 开头）
# 2) Token 权限需包含 Search / Address Autofill 相关能力（默认 Secret token 不含前端 Search 时，请用 Public token 或勾选相应 scopes）
# 3) 在 Mapbox token 的「URL restrictions」中加入：本地 http://localhost:5173、你的生产站点 https://你的域名（与 Vite 实际访问来源一致）
# 4) 本地复制本文件为 .env 并填写；生产在 Railway/Vercel 等「Build / Environment Variables」里设置 VITE_MAPBOX_ACCESS_TOKEN（构建时注入，勿提交到 Git）
# VITE_MAPBOX_ACCESS_TOKEN=pk.xxx
